# Tracker domains

This list contains the first 500 unique, non-IP `||domain^` rules from
[EasyPrivacy](https://easylist.to/easylist/easyprivacy.txt), retrieved on
2026-08-06. `extension/tracker_domains.json` is the packaged runtime copy;
keep it identical to this source file when refreshing the list.
